name: Build livestream worker image

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/livestream-worker-image.yml
      - Dockerfile.livestream-worker
      - .dockerignore
      - app/**
      - scripts/**
      - requirements*.txt
      - pyproject.toml
      - uv.lock
      - poetry.lock

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/weisserw/ibjjf-elo-livestream-worker
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=sha-

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.livestream-worker
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
